import { View, Text} from "./Themed"
import { header } from "../style/styles"
import Colors from "../constants/Colors"
export const Header = (() => {
    return(
        <View style={header}>
        </View>
    )
})